# Hone turn 2026-04-22T022205Z
- target: `scribe`
- outcome: **improved**

## Summary
Tick 17 exercised scribe's measure.cmd from scribe/.hone/hone.yaml for the first time — queued since tick 10. The Bash invocation parsed updates.jsonl and produced a real numeric signal: 2 distinct non-cli callers (hone, hone-session), 6 total updates. That's direct evidence the yaml→bash→signal pipeline works end-to-end for any target that declares a measure command. Separately, synthesized a fresh scribe card: added github_url (public at https://github.com/eidos-agi/scribe), visibility=public, the measure_snapshot from this tick, and a working install sequence (clone + sync + mcp add) that outsiders can actually follow. Also noted license: (none yet — add before external use) in the frontmatter — a real gap for a public repo. Card went from 36 lines to 59 lines of frontmatter carrying the full v0.0.1 state.

## Observations — before
```json
{
  "distinct_non_cli_callers": 2,
  "measure_cmd_executed_before": false,
  "scribe_card_mentions_github_url": false,
  "scribe_card_mentions_visibility": false,
  "scribe_public": true,
  "total_updates_in_jsonl": 6
}
```

## Observations — after
```json
{
  "card_written": true,
  "install_section_added": true,
  "measure_cmd_executed_this_tick": true,
  "measure_direction": "up",
  "measure_signal_numeric": 2,
  "scribe_card_mentions_github_url": true,
  "scribe_card_mentions_license_gap": true,
  "scribe_card_mentions_visibility": true,
  "yaml_to_signal_pipeline_proven": true
}
```

## Delta proposed
Gap surfaced during card synthesis: scribe repo has no LICENSE file — public but unlicensed. Next tick should add one (MIT or Apache-2.0 based on user preference; hone is similarly unlicensed but was published first). Separately: scribe's card_written count in updates.jsonl has now crossed a threshold where scribe_update calls are coming from a real external mover (hone) in addition to the original hone-session CLI; a future tick could add a "callers seen" field to scribe_read output so callers can see who else has been updating a repo's card.
