# Hone turn 2026-04-22T062158Z
- target: `scribe`
- outcome: **improved**

## Summary
Shipped scribe v0.1.0 — the core API change from ADR-004. scribe_update is now path-generic: accepts `(repo, change_summary, path=".scribe/card.md", new_content=None, author_tool=None, new_card=None)`. Any file under the repo can be the destination — README.md, CHANGELOG.md, docs/*, skill files, etc. updates.jsonl now records `path` + `file_written` per entry with `card_written` kept for compat. v0.0.1 callers still work unchanged because `new_card` is a compat alias for `new_content` when path is the default. Added 3 new tests (arbitrary path writes, multi-path log entries, v0.0.1 compat shim); 11/11 pass. CHANGELOG entry landed, version bumped to 0.1.0 in pyproject.toml, v0.1.0 annotated tag created + pushed. Still pending per ADR-004 (future ticks): scribe_review (coherence pass), scribe_suggest (claude -p delegation), .scribe/scribe.yaml config. The core of "scribe is a technical writer" now WORKS in code, not just prose.

## Observations — before
```json
{
  "scribe_can_update_arbitrary_docs": false,
  "scribe_can_update_changelog": false,
  "scribe_can_update_readme": false,
  "scribe_test_count": 8,
  "scribe_update_destination": "hardcoded .scribe/card.md",
  "scribe_update_signature": "(repo, change_summary, new_card?, author_tool?)",
  "scribe_version": "0.0.1",
  "v0_1_0_tag_on_origin": false
}
```

## Observations — after
```json
{
  "adr_004_scope_delivered": "core API (1 of 3 sub-features); scribe_review + scribe_suggest + scribe.yaml still pending",
  "scribe_can_update_arbitrary_docs": true,
  "scribe_can_update_changelog": true,
  "scribe_can_update_readme": true,
  "scribe_test_count": 11,
  "scribe_tests_passing": "11/11",
  "scribe_update_destination": "any file under the repo",
  "scribe_update_signature": "(repo, change_summary, path, new_content?, author_tool?, new_card? [compat])",
  "scribe_version": "0.1.0",
  "v0_0_1_compat_kept": true,
  "v0_1_0_commit_sha": "d09e931",
  "v0_1_0_tag_on_origin": true
}
```

## Delta proposed
Next tick: implement scribe_review(repo) — the coherence-pass tool. Needs a heuristic for "stale": look at git-log for doc files vs git-log for code files, flag docs whose last touch predates N code-touches. Follow-up after that: scribe_suggest(repo, change) which delegates to claude -p. Then .scribe/scribe.yaml for a tracked-docs list. Each is a clean separate tick.
