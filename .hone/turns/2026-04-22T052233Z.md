# Hone turn 2026-04-22T052233Z
- target: `scribe`
- outcome: **improved**

## Summary
Landed scribe TASK-0003 (callers_seen). scribe_read now returns a `callers_seen` field: a sorted list of every distinct author_tool that has ever called scribe_update for the repo, derived on-the-fly from a full scan of updates.jsonl. Pure addition — no signature change, no new storage, no cross-coupling. Two tests added: aggregation across multiple callers (verifies sort + dedup) and empty-state fallback. Scribe test count 6 → 8, all passing. Also closed hone's TASK-0005 (auto-sync guardrails from visionlog) as obsolete — tick 28's cross-coupling removal means auto-syncing at config-load time would reintroduce exactly what we just removed. Note preserved in hone/.ike/tasks/completed/TASK-0005-obsolete.md.

## Observations — before
```json
{
  "hone_task_0005_open": true,
  "hone_task_0005_still_relevant_post_uncoupling": false,
  "scribe_read_returns_callers_seen": false,
  "scribe_test_count": 6
}
```

## Observations — after
```json
{
  "hone_task_0005_status": "obsolete (closed without completion)",
  "scribe_read_returns_callers_seen": true,
  "scribe_signature_compat": "additive \u2014 no break",
  "scribe_test_count": 8,
  "scribe_tests_passing": "8/8"
}
```

## Delta proposed
Remaining open tasks: hone TASK-0002 (PyPI publish), hone TASK-0004 (stepproof cleanup — external, not hone's work), scribe TASK-0002 (PyPI publish). PyPI publishes need account setup + unique names likely (since "hone" and "scribe" are probably taken). Also: the new skill section 6a on bespoke visuals stands ready — noted but not applied to hone/scribe pages yet. Neither obviously needs one (Tool layout is image-free by design), but Daniel could commit to the 5-10 loop iteration for one of them if it's worth the effort.
